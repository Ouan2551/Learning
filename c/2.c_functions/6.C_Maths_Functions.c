#include <stdio.h>
#include <math.h>
int main()
{
    // square root
    printf("%f\n", sqrt(16));
    return 0;

    // round number
    // ceil() => rounds a number upward to nearest integer
    printf("%f\n", ceil(2.5));
}